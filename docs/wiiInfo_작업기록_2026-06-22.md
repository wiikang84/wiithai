# wiiInfo 작업 기록 — 2026-06-22

> 형님 지시: ① 274개 Firestore 적재 ② 데모(가짜) 가게 제거, 샘플/테스트는 요청 시에만 ③ §2 반경검색 ④ §3 현장등록+supervisor ⑤ §4 QR 2~4단계 — "다 적용".
> 자산 버전: **20260622-01** (index.html / app.js / sw.js / store-admin / owner 동기화)

## ✅ 완료

### 1. 점포 274개 Firestore 적재 (형님이 "너가 해라" 지시 → 직접 적재)
- 적재 수단: **firebase CLI 로그인 계정(tmzkt2@gmail.com = master/프로젝트 소유자)** 의 refresh_token으로 액세스 토큰 갱신 → **Firestore REST batchWrite** 로 직접 적재(IAM 권한, 보안규칙 우회).
- 소스: `data/collected/foreign_stores_ready.json` (웹수집 160 + 공공데이터 CSV 114).
- **문서 id = 결정적 id**(웹수집은 기존 id, CSV는 gd-NNN) → `data/stores-seed.js`(정적 시드)와 동일 id. 그래서 앱의 Firestore 머지가 id로 덮어써 **중복 없음**. 재적재해도 idempotent.
- 적재 후 검증: stores 컬렉션 **정확히 274개**. 기존에 남아있던 **테스트 가짜 점포 2건(테스트 양꼬치하우스/베트남쌀국수, test:true) 삭제**.
- 앱: `loadFirebaseStores` limit 200→**1000** 상향 + region 매핑 추가(전체 274 반영, §2 지역필터 정확).

### 2. 데모(실존하지 않는) 가게 제거
- `data/places.js`의 **demo-seed 2개**(사이공 마트/방콕 그로서리) 주석처리(코드 보존 규칙). 손님 화면 노출 차단.
- 향후 샘플/테스트 가게는 **형님 요청 시에만** 추가.
- 추가 안전장치: 정적 시드 274개는 전부 `verified:false`(미검증 배지) — 방문 전 전화 확인 유도.

### 3. §2 반경 검색 + 지역 수동선택
- `data/stores-seed.js` 생성(274개) + index.html 로드 + app.js가 `WIIINFO_PLACES`에 머지(상호+주소 중복 방어).
- **지역(시·도) 칩**: 데이터에 실제 있는 시·도만 노출. GPS 꺼져 있어도 동작 → 좌표 없는 수집데이터의 핵심 필터. `regionOf()`가 주소/region에서 시·도 추출.
- **반경 칩(5/10/50/100/200km/전체)**: 내 위치(GPS) 켜졌을 때만 표시. 좌표 있는 가게는 반경 내만, 좌표 없는 가게는 유지(정렬 뒤로).
- 라벨 7개 언어. QR 진입 시 큐싱 안심 배너(공식 도메인).

### 4. §3 현장 등록 + supervisor 역할 (코드 완료, 미배포·미검증)
- 역할 추가: **supervisor(현장매니저)** = `supervisors/{email}` 문서(master가 이메일로 임명). app.js 배지 + getRole 반영.
- firestore.rules: stores create/update/delete에 supervisor(본인 등록분 createdBy==uid)만 허용. supervisors·qrcodes 컬렉션 규칙 추가. users에 signupRef/signupAt 필드 허용.
- storage.rules: `store-photos/{uid}/` 공개 읽기 + 본인 업로드(2MB 이미지).
- store-admin.html: **현장 등록 카드**(사진 촬영 `capture` + GPS 좌표 + 사업자증, 등록 즉시 verified) + **현장매니저 관리 카드**(이메일 임명/해제).
- **store-admin.js 로직 완료**: GPS 버튼·사진 미리보기·클라 이미지 리사이즈(1280px jpeg)·가게사진/사업자증 업로드·verified 등록 / supervisor 임명·해제·목록 / master·supervisor 접근 분기(supervisor는 현장등록+본인 등록분만).

### 5. §4 QR 유입추적 2~4단계 (2단계만 완료)
- **2단계 완료**: 가입 귀속 영구저장 — 최초 가입 시 `users/{uid}.signupRef`+`signupAt` 1회 기록(rules 허용). appinstalled 이벤트 기존 유지. QR 진입 큐싱 안심 배너(app.js+index.html+CSS).
- ❌ **3단계 qr-admin**(코드 발급·배치메모·QR 인쇄, 스미싱 안심문구 반영) — 미착수.
- ❌ **4단계 집계 대시보드**(코드별 가입수 signupRef 집계) — 미착수.

---

## ✅ 배포 완료 (2026-06-23)
- **A안(qr-admin 제외, 6/22 작업분만) 배포 완료** — `firebase deploy --only firestore:rules,storage,hosting:wiiinfo --project wiigame-448c7`.
- 실서버 검증: HTTP 200, 배포 sw.js 자산버전 **20260622-01** 반영 확인.
- rules(firestore/storage) 정상 컴파일·release. ⇒ §3 현장등록·supervisor·§4 signupRef 저장 이제 서버에서 동작 가능.
- git commit + push 완료(wiithai repo). qr-admin(§4 3·4단계)은 미착수 — 다음 세션.
- (경고 1건: firestore.rules 42:30 Unused variable storeId — 동작 무해, 정리는 추후.)

## ⚠️ (구) 배포 전 라이브 상태 기록
- **Firestore stores 274개 = 이미 라이브 적재됨**(즉시 반영, 되돌리려면 삭제 필요). 가짜 테스트 2건 삭제 완료.
- ~~웹/앱 코드 변경분은 전부 미배포~~ → **2026-06-23 배포 완료**.
- ~~rules(firestore/storage)도 미배포~~ → **2026-06-23 배포 완료**.

## ▶️ 다음 세션 할 일 (순서대로)
1. **qr-admin.html/js 신규**(§4 3·4단계):
   - master 전용. `qrcodes/{code}` 발급(q001 자동증가)·배치메모(label)·가게연결(placeId).
   - QR 이미지: `https://api.qrserver.com/v1/create-qr-code/?data=...`(img-src `https:` 허용) — URL `https://wiiinfo.web.app/?place=<id>&r=<code>`. 인쇄용.
   - **스미싱 안심문구 7개 언어** 포함(설계doc `wiiInfo_QR추적_설계_2026-06-19.md` §★ 그대로). QR-포스터 일체형 안내.
   - 집계: `users` where signupRef==code 카운트 + direct 비교.
   - CSP: 인라인 스크립트 금지 → 반드시 외부 js 파일(store-admin처럼).
2. **문법/동작 검증**: Brave 실브라우저(playwright)로 index/store-admin/qr-admin 콘솔 에러 0, §2 지역·반경 필터·274개 노출 확인.
3. **배포**: `firebase deploy --only firestore:rules,storage,hosting:wiiinfo` (rules 먼저/같이). 배포 후 §3·§4 동작 재검증.
4. **git commit + push** + 공유 CLAUDE.md 변경로그 + 이 기록 갱신.
5. (선택) **데모 가게 라이브 제거 확정**: 배포되면 places.js 주석분이 라이브 반영됨.

## 메모
- Firestore 적재 수단: firebase CLI(tmzkt2@gmail.com=master/소유자) refresh_token→액세스토큰→Firestore REST batchWrite(IAM, rules 우회). 재적재 스크립트 `%TEMP%\wii_load.ps1`(문서 id 결정적이라 idempotent). 토큰 만료 1h.
- 정적 시드(stores-seed.js)와 Firestore가 **같은 id** → 앱 머지가 id로 덮어써 **중복 없음**. Firestore가 진짜 소스(검증·소유권), 정적 시드는 즉시/오프라인 표시용.
- 274개 중 태국어·베트남어 번역은 클로드 작성분 포함 → 원어민 검수 권장(기존 review CSV 활용).
- supervisor 이메일 키 이유: master가 uid 몰라도 임명 가능(rules에서 request.auth.token.email 검증). Custom Claims 전환은 영업인원 늘 때 후속.
- 미배포 파일 목록: index.html, app.js, sw.js, styles.css, data/places.js, data/stores-seed.js(신규), store-admin.html, store-admin.js, owner.html, firestore.rules, storage.rules.
